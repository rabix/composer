export interface SyncConfig {
    /**
     * Determines whether updating the code content stream will trigger a content update as if the change is made manually
     */
    emitCodeChange?: boolean
}
