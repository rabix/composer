declare const ENV_PARAMS;

export const ENVP: {
    user: {
        sessionId: string
    },
    apiUrls: {
        brood: string,
        watson: string,
        gatekeeper: string
    }
} = ENV_PARAMS;
